import React from 'react';
import MainContainer from '@/views/MainContainer';

const Home = (props) => {
    return (
        <>
            <MainContainer />
        </>
    );
};

export default Home;
