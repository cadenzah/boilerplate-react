import React from 'react';
import AboutContainer from '@/views/AboutContainer';

const About = (props) => {
    return (
        <>
            <AboutContainer />
        </>
    );
};

export default About;
